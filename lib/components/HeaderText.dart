import 'package:flutter/material.dart';

class HeaderText extends StatelessWidget {
  const HeaderText({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      "Quebrei!",
      style: TextStyle(color: Color(0xFF0e9094), fontSize: 20),
    );
  }
}
